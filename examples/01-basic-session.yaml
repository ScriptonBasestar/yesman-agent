# Basic Session Configuration
# Minimal configuration for a single development session

session_name: "my-project"
description: "Basic development session"

# Minimal logging
logging:
  level: "INFO"
  file: "~/.scripton/yesman/logs/basic.log"